const numeros = [100, 200, 300, 400, 500, 600]
                    //6
for (let i = 0; i < numeros.length; i++){ // ultima ação executada é o i++ (i= i+1)
            //i=0 numeros no indice [0]
    console.log (i, numeros[i])
} 